# Example .env file for local development
# Copy to `.env` and fill values when developing locally (don't commit `.env`).

# Shop Bot Token (Telegram @BotFather)
SHOP_BOT_TOKEN=your_shop_bot_token_here

# Files Bot Token (optional - for file distribution bot)
FILES_BOT_TOKEN=your_files_bot_token_here
